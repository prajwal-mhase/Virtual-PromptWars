# Docs Style Guide

- Use consistent heading levels across docs.
- Prefer short sections with clear titles.
- Keep code fences annotated (`bash`, `json`, `mermaid`).
- Keep relative links (e.g. `./api.md`) when referencing within the repo.

